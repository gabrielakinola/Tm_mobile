# Optional override for the backend API URL.
# Required only if auto-detection does not work on your network.
# Use your computer's local IP when testing on a physical device with Expo Go.
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.42:3000/api
EXPO_PUBLIC_API_URL=
